export { default as testSenderMiddleware } from '@middlewares/form.fetcher.middleware';

export { default as testFetcherMiddleware } from '@middlewares/socket.fetcher.middleware';

export { default as formFetcherMiddleware } from '@middlewares/test.fetcher.middleware';

export { default as socketMiddleware } from '@middlewares/test.sender.middleware';

export { default as roomMiddleware } from '@middlewares/room.fetcher.middleware';

export { default as gameMiddleware } from '@middlewares/game.fetcher.middleware';
